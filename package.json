{
  "name": "listies",
  "version": "1.0.0",
  "main": "server.ts",
  "author": "J Quinn",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "config": "^3.2.0",
    "dotenv": "^8.1.0",
    "express": "^4.17.1",
    "express-validator": "^6.1.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.6.4",
    "socket.io": "^3.0.0"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "nodemon": "^2.0.6"
  },
  "scripts": {
    "start": "node server.js",
    "server": "nodemon server.js",
    "client": "cd client; yarn start",
    "clientInstall": "cd client; yarn install",
    "dev": "concurrently \"yarn start\" \"yarn client\"",
    "devInstall": "yarn install; yarn clientInstall",
    "devUpgrade": "yarn upgrade; cd client; yarn upgrade",
    "heroku-postbuild": "cd client && yarn && yarn run build"
  },
  "repository": {
    "type": "git"
  }
}
